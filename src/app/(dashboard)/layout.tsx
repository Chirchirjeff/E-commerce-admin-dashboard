/*
 * Dashboard Layout - Wraps all dashboard pages
 * Ensures consistent layout structure across all dashboard pages
 */

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}